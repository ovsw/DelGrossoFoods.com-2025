#!/usr/bin/env sh
set -e

# Run lint-staged quietly and without stashing to avoid confusing UI popups
pnpm -w lint-staged --no-stash --quiet

# Block commit if type errors exist in the workspace
pnpm -w turbo run check-types
