#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

set -e

echo "Running workspace format check (Prettier)…"
pnpm -w run format:check

echo "Running workspace lint (Turbo)…"
pnpm -w turbo run lint

echo "Running workspace type checks (Turbo)…"
pnpm -w turbo run check-types
