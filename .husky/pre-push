#!/bin/sh
set -e

# Husky v10-compatible simple hook (no sourcing husky.sh)
REPO_ROOT="$(git rev-parse --show-toplevel)"
echo "Running lint on all workspaces..."

# Run turbo lint via pnpm
pnpm lint || { echo "Lint failed. Aborting push."; exit 1; }
