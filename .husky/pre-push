#!/bin/sh
set -e

# Husky v10-compatible simple hook (no sourcing husky.sh)
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT/apps/web"
echo "Running a11y lint (apps/web)…"
pnpm run lint || { echo "A11y lint failed. Aborting push."; exit 1; }
