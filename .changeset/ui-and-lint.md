---
"@workspace/ui": minor
"@workspace/eslint-config": patch
---

Shared UI now owns the hero shells, catalog layouts, Where To Buy client and the presentation data-attribute helpers so both sites reuse the same polish, and ESLint keeps pace with Next 15.5.9 via the matching plugin bump.
