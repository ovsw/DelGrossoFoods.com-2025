---
"web": minor
---

Add SSR + client-filterable Recipes index at `/recipes` with filters for Product Line, Recipe Tags, Meat, Category, and search; include `/recipes/[slug]` stub. Reuse shared FilterableListLayout and ListCard, and add recipe queries + types.
